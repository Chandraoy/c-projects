#include <iostream>
using namespace std;

int main(){
	
	//integer (whole number)
	int age = 21;
	int years =2023;
	int days = 7.5;   // its only show the first number of the point in the decimal number
	
	cout<<days; //7 (the result only show the integer part)
	
	
	//double (number include decimal)
	double price = 10.99;
	double  gpa= 2.5;
	double temperature= 25.1;
	
	
	//char ( single character)
	char grade = 'A';
	char initial ='B';
	char currency = '$';
	char example = 'BC' ; // this code only run one character, which stand last in the data value.
	
	cout<< 'exmple'; // output will be C, bcz char data type can enter multiple character!!
	
	
	//booleam ( its only TRUE/ FALSE)
	bool student = false; //0
	bool power = true;//1
	bool forSale = true;//1
	
	// bool only show the output in (0/1)
	// if its true, the output is '1' and the false will be '0'.
	
	
	
	// string ( objects that represents a sequence of text)
     string name = "bro";
     string day =" friday ";
     string food = " pizza ";
     string address = " 123 fake st.";
     
     cout<< name;
    
	
	
}